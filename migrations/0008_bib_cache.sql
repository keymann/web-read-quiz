-- 0008_bib_cache.sql — 조사 단계에서 받은 서지 결과를 그 책에 붙여 둔다
--
-- 조사 1회에 서지 조회가 두 번 돌고 있었다. 계획(§4.6)을 만들 때 지적한 낭비인데, 브라우저
-- 릴레이 경로에서는 요청이 둘로 갈려(plan → apply) 메모리로 넘길 방법이 없다.
--
-- 낭비보다 중요한 문제가 있다. **프롬프트가 본 서지와 병합에 쓰는 서지가 다를 수 있다.**
-- 두 번 부르는 사이에 외부 API 응답이 바뀌면 모델은 A 를 보고 답했는데 서버는 B 로 제목·저자를
-- 덮어쓴다. 한 번 받은 것을 적어 두고 양쪽이 같은 값을 쓰게 한다.
--
-- 캐시라기보다 **그 조사의 입력 기록**이다. 다음 조사에서는 새로 받아 덮어쓴다.

ALTER TABLE books ADD COLUMN bib_cache TEXT;
