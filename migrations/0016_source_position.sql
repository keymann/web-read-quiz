-- 참고 자료를 화면에 늘어놓을 순서.
--
-- 부모가 근거를 훑는 순서는 정해져 있다 — 카카오 책 → 알라딘 → 웹 검색. 앞의 둘은 서지
-- 데이터베이스로 검증된 정보이고, 웹 검색은 그다음에 참고할 것들이다.
--
-- `created_at` 으로는 이 순서를 지킬 수 없다. 한 배치에서 넣으면 밀리초까지 같은 값이 박혀
-- 정렬이 사실상 무순서가 된다(rowid 순으로 나오는 것은 구현에 딸린 우연이다). id 는 무작위라
-- 기댈 수 없다. 그래서 넣는 쪽이 순서를 적어 둔다.
ALTER TABLE book_sources ADD COLUMN position INTEGER NOT NULL DEFAULT 0;
